name: Build and Deploy Gradio App to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: "gradio-pages"
  cancel-in-progress: false

env:
  PYTHON_VERSION: "3.10"
  GRADIO_VERSION: "4.44.0"  # ✅ stable version supporting export

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: "pip"

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install gradio==${{ env.GRADIO_VERSION }}
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          fi

      - name: 🏗️ Export Gradio App to Static Site
        run: |
          mkdir -p build
          # ✅ Correct export command for Gradio >=4.37
          gradio export build/ --app app:app

      - name: 📤 Upload Artifact for Deployment
        uses: actions/upload-pages-artifact@v3
        with:
          name: gradio-build-artifact
          path: build

  deploy:
    needs: build
    runs-on: ubuntu-latest

    permissions:
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: ⚙️ Setup GitHub Pages
        uses: actions/configure-pages@v5

      - name: 🚀 Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        with:
          artifact_name: gradio-build-artifact
